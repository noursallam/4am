---
import BtnLink from '../shared/BtnLink.astro';

export interface Props {
    behanceUrl: string
}

const { behanceUrl = "https://www.behance.net/4am-agency" } = Astro.props;
---

<div class="relative w-full h-auto bg-gradient-to-br from-indigo-600 to-blue-700 rounded-3xl overflow-hidden shadow-xl border border-box-border p-10 md:p-16 transition-all duration-300 hover:shadow-2xl hover:-translate-y-2 group"
    data-aos="fade-up">
    
    <!-- Behance Logo Background (semi-transparent) -->
    <div class="absolute top-0 left-0 w-full h-full opacity-10 flex items-center justify-center overflow-hidden">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" class="w-96 h-96 text-white transform -rotate-12">
            <path fill="currentColor" d="M10 10.2H6V9h4v1.2zM16.5 17h-5v-1.2h5V17zM20.9 10.9c-.5-.5-1.3-.9-2.2-.9-1.6 0-2.8.8-3.3 2.1h-.1c-.3-1.4-1.4-2.1-2.9-2.1-1.1 0-2 .4-2.7 1.1v-.7H7.3V18H10V13.8c0-1.1.8-1.9 1.8-1.9 1.1 0 1.8.8 1.8 1.9V18h2.8v-4.2c0-1.1.8-1.9 1.8-1.9s1.8.8 1.8 1.9V18h2.8v-4.5c0-1-.3-1.9-.9-2.6zM7.7 6H2.8v11.9h5.1c2.4 0 3.8-1.3 3.8-3.5 0-1.5-.9-2.4-1.5-2.7.5-.4 1.1-1.2 1.1-2.3-.1-1.6-1.1-3.4-3.6-3.4zm-2.7 2h2.3c1 0 1.4.6 1.4 1.4 0 1-.7 1.4-1.6 1.4H5V8zm2.4 8H5v-3.1h2.4c1.2 0 1.8.7 1.8 1.6 0 .8-.7 1.5-1.8 1.5z"/>
        </svg>
    </div>
    
    <div class="relative z-10 flex flex-col items-center text-center">
        <!-- Behance Icon -->
        <div class="mb-8 bg-white rounded-full p-6 w-28 h-28 flex items-center justify-center shadow-lg">
            <img src="/logos/behance-1.svg" alt="Behance Logo" class="w-16 h-16" />
        </div>
        
        <!-- Text Content -->
        <h3 class="text-3xl md:text-4xl font-bold text-white mb-6">معرض اعمالنا</h3>
        <p class="text-white/90 text-lg md:text-xl mb-10 max-w-2xl">نقدم مجموعة متنوعة من المشاريع الإبداعية على منصة Behance. اكتشف أعمالنا المميزة في مجالات التصميم والهوية البصرية والتسويق الرقمي، وكيف نساعد العملاء على تحقيق رؤيتهم بأسلوب إبداعي وفريد.</p>
        
        <!-- Animated Shapes -->
        <div class="absolute top-16 right-8 w-16 h-16 rounded-full bg-white/10 animate-float"></div>
        <div class="absolute bottom-20 left-10 w-24 h-24 rounded-full bg-white/10 animate-float-delay"></div>
        <div class="absolute top-1/3 left-16 w-12 h-12 rounded-full bg-white/10 animate-float-slow"></div>
        
        <!-- CTA Button -->
        <BtnLink 
            text="زيارة صفحتنا على Behance" 
            href={behanceUrl} 
            variant="inverted"
            className="group-hover:bg-white group-hover:text-blue-700 transition-all duration-300 text-lg px-8 py-4"
            target="_blank"
        />
    </div>
</div>

<style>
    @keyframes float {
        0% { transform: translateY(0px); }
        50% { transform: translateY(-20px); }
        100% { transform: translateY(0px); }
    }
    
    @keyframes floatDelay {
        0% { transform: translateY(0px); }
        50% { transform: translateY(-15px); }
        100% { transform: translateY(0px); }
    }
    
    @keyframes floatSlow {
        0% { transform: translateY(0px) translateX(0px); }
        33% { transform: translateY(-10px) translateX(10px); }
        66% { transform: translateY(5px) translateX(-5px); }
        100% { transform: translateY(0px) translateX(0px); }
    }
    
    .animate-float {
        animation: float 6s ease-in-out infinite;
    }
    
    .animate-float-delay {
        animation: floatDelay 7s ease-in-out infinite;
        animation-delay: 1s;
    }
    
    .animate-float-slow {
        animation: floatSlow 9s ease-in-out infinite;
        animation-delay: 2s;
    }
</style>
